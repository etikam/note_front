/** Colonnes obligatoires CSV enseignants (aligné notes-api apps.teachers.services.teacher_import). */
export const TEACHER_IMPORT_REQUIRED_COLUMNS = ['matricule', 'first_name', 'last_name', 'email']
