/**
 * Types communs pour les réponses et erreurs API (documentation).
 * Réponse paginée DRF : { count, next, previous, results }
 * Erreur validation : { [field]: string[] } ou { detail: string }
 */

// Pas d'exports de types en JS ; utiliser ces structures dans les appels API.
export const API = {}
